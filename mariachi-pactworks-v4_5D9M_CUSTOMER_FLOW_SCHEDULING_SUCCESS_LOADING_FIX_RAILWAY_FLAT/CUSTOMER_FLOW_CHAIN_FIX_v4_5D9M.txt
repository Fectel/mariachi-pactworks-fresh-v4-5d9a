PactWorks v4.5D9M

- Quote + Scheduling is published to the public pricing config in both admin copies.
- Quick quote uses apiBase (the actual public/runtime API field).
- A blank stored API override no longer suppresses a valid Railway PUBLIC_REQUEST_API_URL.
- Customer quote success exits the form into a dedicated success screen.
- Success screen distinguishes hosted delivery vs local-only save.
- Quote, availability, auth email, and full request submission buttons show loading/disabled states.
- "See prices first" and "Request → Reserve" splash cards now both open /pricing/.
- Existing top View Pricing button continues to open /pricing/.
- No database changes.
